[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "learn-python-cli"
version = "0.1.0"
dependencies = [
    "typer[all]",
]

[project.scripts]
mycli = "playground:app"

[tool.setuptools]
py-modules = ["playground"]
