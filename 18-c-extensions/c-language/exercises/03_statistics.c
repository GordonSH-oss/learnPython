#include <stdio.h>

int main(void) {
    /* TODO: 为数组实现并调用 min、max、mean 函数。 */
    int values[] = {8, 3, 11, -2, 6};
    printf("TODO: %zu values\n", sizeof values / sizeof values[0]);
    return 0;
}
